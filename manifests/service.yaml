apiVersion: v1
kind: Service
metadata:
  name: iot-service
  namespace: dev
spec:
  type: NodePort
  selector:
    app: wil42-playground
  ports:
    - protocol: TCP
      nodePort: 8888
      port: 8888
      targetPort: 8888
